name: ql_gen
description: A library for generating dart code from GraphQL schema.
version: 0.1.4
repository: https://github.com/roadroot/ql_gen

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  args: ^2.7.0
  tokenizer_parser: ^0.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
